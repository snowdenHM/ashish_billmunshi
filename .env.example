# ---------------------------
# Django Application Settings
# ---------------------------

# Secret key used for Django's cryptographic signing. Keep this secret in production.
SECRET_KEY="django-insecure-vTA7Mk3amTPdLCSuduER9I0o2n0KKTBst9j31kIO"

# ---------------------------
# Database Configuration
# ---------------------------

# PostgreSQL database connection URL in the format:
# postgresql://<username>:<password>@<host>:<port>/<database_name>
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/base"

# ---------------------------
# Redis Configuration
# ---------------------------

# Redis server URL (used for caching, Celery broker, etc.).
REDIS_URL="redis://localhost:6379"

# ---------------------------
# CORS (Cross-Origin Resource Sharing)
# ---------------------------

# Comma-separated list of origins allowed to make requests to this server.
# Used for local frontend development (e.g., Vite/React).
CORS_ALLOWED_ORIGINS="http://localhost:5174,http://127.0.0.1:5174"

# ---------------------------
# Storage Settings
# ---------------------------

# Boolean flag to determine whether to use AWS S3 for storing media files.
USE_S3_MEDIA=False

# AWS S3 bucket name (required if USE_S3_MEDIA=True).
AWS_STORAGE_BUCKET_NAME=

# AWS access key ID for S3 access.
AWS_ACCESS_KEY_ID=

# AWS secret access key for S3 access.
AWS_SECRET_ACCESS_KEY=

# ---------------------------
# Email Configuration
# ---------------------------

# Default sender email address for outgoing messages.
DEFAULT_FROM_EMAIL="heramb1008@gmail.com"

# Server email address used for error notifications.
SERVER_EMAIL="noreply@localhost:8000"

# Django email backend for sending emails (here using Mailgun via Anymail).
EMAIL_BACKEND="anymail.backends.mailgun.EmailBackend"

# Mailgun API key for authentication.
MAILGUN_API_KEY="key-***"

# Mailgun sender domain for sending emails.
MAILGUN_SENDER_DOMAIN="example.com"
